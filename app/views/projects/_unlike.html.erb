<%= form_for(current_user.likes.find_by(project_id: project.id),
             html: { method: :delete }, remote: true) do |f| %>

	<button type="submit" class="unlike">
  		<i class="fa fa-thumbs-o-up fa-2x"></i> 
  	</button>

<% end %>